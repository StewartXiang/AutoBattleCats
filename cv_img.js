"nodejs";
const cv = require('opencv4nodejs');
function detect_color(img, proc) {
    mat = img.mat
    cols = mat.cols
    rows = mat.rows
    emptyMat = cv.Mat(rows, cols, CV_8UC1)
}

function test_cv_img() {
    
}